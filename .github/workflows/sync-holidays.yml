name: Sync Religious Holidays Daily

on:
  schedule:
    - cron: '0 2 * * *'  # Daily at 2 AM UTC
  workflow_dispatch:

jobs:
  sync-holidays:
    runs-on: ubuntu-latest
    
    steps:
      - name: Sync holidays for user
        run: |
          curl -X POST https://wish-loop-4ab4d767.base44.app/api/apps/6902d23cfff0eb754ab4d767/functions/syncReligiousHolidays \
            -H "Content-Type: application/json" \
            -H "api_key: ${{ secrets.BASE44_API_KEY }}" \
            -d '{"user_email": "yochai1235@gmail.com", "days_ahead": 60}'
